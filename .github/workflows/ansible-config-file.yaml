name:  Configuration ansible.cfg 

on:
  workflow_dispatch:

jobs:
  generate-ansible-cfg:
    runs-on: [ self-hosted ]

    steps:


      - name: Generate ansible.cfg
        run: |
          sudo -u robert bash -c 'cat > /home/robert/ansible/ansible.cfg' << 'EOF'
          [defaults]
          inventory      = /home/robert/ansible/inventory.yaml
          roles_path    = /home/robert/ansible/roles
          host_key_checking = False
          remote_user = robert
          private_key_file = /home/robert/.ssh/id_rsa
          log_path = /home/robert/ansible/ansible.log
          forks = 5
          gathering = smart
          fact_caching = jsonfile
          fact_caching_connection = /home/robert/ansible/facts_cache
          

          [privilege_escalation]
          become = True
          become_method = sudo
          become_user = root
          become_ask_pass = False

          [ssh_connection]
          ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o ConnectTimeout=10
          control_path = /home/robert/.ssh/id_rsa-%%r@%%h:%%p
          EOF
          

      - name: Set proper permissions
        run: |
          sudo chown robert:robert /home/robert/ansible/ansible.cfg
          sudo chmod 644 /home/robert/ansible/ansible.cfg

      - name: Verify configuration
        run: |
          echo "Ansible configuration:"
          sudo -u robert ls -la /home/robert/ansible/ansible.cfg
          echo -e "\nContents:"
          sudo -u robert cat /home/robert/ansible/ansible.cfg
