name: Ansible Connection

on:
  workflow_dispatch:

jobs:
  test-ansible-connections:
    runs-on: [ self-hosted ]

    steps:
    
      - name: Test connections with ping module
        run: |
          ansible all --list-hosts -i /home/robert/ansible/inventory.yaml
          ansible all -m ping -i /home/robert/ansible/inventory.yaml
